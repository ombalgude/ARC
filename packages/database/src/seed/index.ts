import "dotenv/config";

import { db } from "../client.js";

async function main() {
  void db;
  console.info("[@arc/database] Seed infrastructure is ready. No seed data is configured yet.");
}

void main();
